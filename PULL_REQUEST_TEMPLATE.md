## What does this change do?

<!-- A short description of the change and why it's needed. Link any related issue with "Closes #123". -->

## How was this tested?

<!-- e.g. "Loaded unpacked, recorded a 30s clip in WebM and WAV, verified playback in history page." -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run build` completes successfully
- [ ] Manually tested the affected flow by loading the unpacked extension
- [ ] No stray build artifacts committed (`git status` is clean of generated `*.js`/`dist/`/`release/`)
- [ ] Updated the README/docs if behavior, permissions, or setup steps changed
